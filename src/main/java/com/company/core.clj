(ns com.company.core
  (:require
    [clojure.java.io :as io]
    [clojure.pprint :as pp]
    )
  (:gen-class))

(defn -main
  "Main"
  [& args]
  (println "Hello, World!"))

(defn foo [] (println "test1"))

